import { HeroShortListPipe } from './hero-short-list.pipe';

describe('HeroShortListPipe', () => {
  it('create an instance', () => {
    const pipe = new HeroShortListPipe();
    expect(pipe).toBeTruthy();
  });
});
