import { GamesOnCertainPlatformPipe } from './games-on-certain-platform.pipe';

describe('GamesOnCertainPlatformPipe', () => {
  it('create an instance', () => {
    const pipe = new GamesOnCertainPlatformPipe();
    expect(pipe).toBeTruthy();
  });
});
